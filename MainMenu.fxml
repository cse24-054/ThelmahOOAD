<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.text.Font?>

<VBox alignment="TOP_CENTER" prefHeight="450.0" prefWidth="600.0"
      spacing="15"
      style="-fx-background-color: #11366D;"
      xmlns="http://javafx.com/javafx/23.0.1"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.bankingsystem.MainMenuController">

    <padding>
        <Insets top="40" right="50" bottom="30" left="50"/>
    </padding>

    <Label text="MAIN MENU" textFill="#f8f1f1">
        <font>
            <Font name="System Bold Italic" size="40.0" />
        </font>
        <VBox.margin>
            <Insets bottom="15.0" />
        </VBox.margin>
    </Label>

    <!-- Accounts Button -->
    <Button fx:id="AccountsButton" text="💰 My Accounts"
            onAction="#handleAccountsPage"
            prefHeight="50.0" prefWidth="350.0"
            style="-fx-background-color: #38a169; -fx-text-fill: white; -fx-font-weight: bold; -fx-cursor: hand; -fx-font-size: 16pt; -fx-background-radius: 10;" />

    <!-- Transactions Button (REPLACED Statements) -->
    <Button fx:id="TransactionsButton" text="📈 View Transactions"
            onAction="#handleTransactionsPage"
            prefHeight="50.0" prefWidth="350.0"
            style="-fx-background-color: #4299e1; -fx-text-fill: white; -fx-font-weight: bold; -fx-cursor: hand; -fx-font-size: 16pt; -fx-background-radius: 10;" />

    <!-- Settings Button -->
    <Button fx:id="SettingsButton" text="⚙️ Settings"
            onAction="#handleSettingsPage"
            prefHeight="50.0" prefWidth="350.0"
            style="-fx-background-color: #f6ad55; -fx-text-fill: #11366D; -fx-font-weight: bold; -fx-cursor: hand; -fx-font-size: 16pt; -fx-background-radius: 10;" />

    <!-- Logout Button -->
    <Button fx:id="LogoutButton" text="← Log Out"
            onAction="#handleLogout"
            prefHeight="40.0" prefWidth="200.0"
            style="-fx-background-color: #e53e3e; -fx-text-fill: white; -fx-font-size: 14pt; -fx-font-weight: bold; -fx-background-radius: 5;">
        <VBox.margin>
            <Insets top="30.0" />
        </VBox.margin>
    </Button>

</VBox>